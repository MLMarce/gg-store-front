import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["encrypted-tbn3.gstatic.com", "raw.githubusercontent.com"],
  },
};

export default nextConfig;
